# Installation Guide

To integrate this skill:
1. Clone the repository into your skills directory.
2. Verify all files are structured properly.
3. Configure the `config/defaults.yaml` and `config/source-policy.yaml` files.
